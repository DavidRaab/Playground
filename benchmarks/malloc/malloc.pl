#!/usr/bin/env perl
use v5.36;
#use open ':std', ':encoding(UTF-8)';
#use Data::Printer;
#use Getopt::Long::Descriptive;

for (my $i=0; $i < 100_000_000; $i++) {
    my $vec = { x => 10, y => 10 };
}
